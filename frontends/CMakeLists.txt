# Frontend compilers - ROM knowledge lives here
# Each frontend emits GameDefinition + Assets for the engine

add_subdirectory(crystal)

# Future frontends:
# add_subdirectory(gen1)
# add_subdirectory(polished_crystal)
